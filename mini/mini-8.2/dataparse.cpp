// (c) by Stefan Roettger

#include "minibase.h"

#include "dataparse.hpp"

// default constructor
dataparse::dataparse()
   {
   }

// destructor
dataparse::~dataparse()
   {
   }

// set the program to be parsed and executed
void dataparse::setprogram(char *program)
   {
   }

// completely reset the parser
void dataparse::reset()
   {
   }

// push one value onto the computation stack
void dataparse::pushvalue(float v)
   {
   }

// pop one value from the computation stack
float dataparse::popvalue()
   {return(0.0f);}

// completely reset the interpreter
void dataparse::init()
   {
   }

// execute the previously parsed program
void dataparse::execute()
   {
   }
